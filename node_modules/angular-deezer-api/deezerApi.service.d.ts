import { Http, Jsonp } from '@angular/http';
import 'rxjs/add/operator/toPromise';
export declare class DeezerApiService {
    private http;
    private _jsonp;
    private searchUrl;
    constructor(http: Http, _jsonp: Jsonp);
    search(value: any): Promise<any>;
    private handleError(error);
}
